{
  "name": "tpg_code_project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:watch": "sass --watch ./src/scss:css",
    "serve": "browser-sync start --server '.'  --files '.'",
    "watch:css": "onchange './src/scss/*.scss' -- npm run sass:watch",
    "start": "concurrently \"npm run serve\" \"npm run watch:css\""
  },
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^6.2.0",
    "sass": "^1.34.1"
  },
  "devDependencies": {
    "browser-sync": "^2.26.14",
    "onchange": "^7.1.0"
  }
}
